# Spec Directory

The full PRD will live in `./spec.md`.

This directory also holds:
- `data-model.md` — Supabase schema documentation
- `api-contracts.md` — module boundaries and contracts
- `decisions/` — Architecture Decision Records (ADRs)
